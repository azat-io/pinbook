# Shapes

Use `id` values from this category when setting `pin.icon`.

Rules:

- `id` is the canonical value to emit.
- `name` is only a human hint for selection.
- `code` exists for debugging and Google My Maps / KML matching.

| id                    | name           | code   |
| --------------------- | -------------- | ------ |
| `shapes-square-small` | Square (Small) | `1498` |
| `shapes-circle`       | Circle         | `1499` |
| `shapes-square`       | Square         | `1500` |
| `shapes-diamond`      | Diamond        | `1501` |
| `shapes-star`         | Star           | `1502` |
| `shapes-heart`        | Heart          | `1592` |
| `shapes-point`        | Point          | `1739` |
| `shapes-checkmark`    | Checkmark      | `1769` |
| `shapes-quotation`    | Quotation      | `1846` |
| `shapes-x`            | X              | `1898` |
| `shapes-pin`          | Pin            | `1899` |
